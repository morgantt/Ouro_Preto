
<script type="text/javascript">
jQuery(function($j) {
  // when the #operadora field changes
  $j("#cparametro_operadora_id").change(function() {
    // make a POST call and replace the content
    var operadora = $j('select#cparametro_operadora_id :selected').val();
     if(operadora == "") operadora="0";
	//		alert(operadora)
    jQuery.get('/cparametros/update_tipo_div/' + operadora, function(data){
       
    })
    return false;
  });

})
</script>




<%= render 'shared/error_messages', :object => f.object %>

<div class='field'>
  <%= f.label :Corretor %><br />
  <%= f.collection_select("corretor_id", Corretor.find(:all, :conditions => ["id = ?", @corretor]), :id, :nome) %>
</div>
<div class='field'>

  <%= f.label :Operadoraid %><br />
  <%= f.collection_select( "operadora_id", @operadora, :id, :nome,
  										 {:prompt => 'Por Favor selecione a operadora'}) %>
  										 
</div>
<div id='tipo'>

<%= f.label :tipoproposta %><br />
<%= f.collection_select("tipoproposta_id", @tipoproposta, :id, :tipo,
  										 {:prompt => 'selecione o Tipo de Proposta.'}) %>

</div>


<div class='field'>
  <%= f.label :Maximo %><br />
  <%= f.text_field :maximo %>
</div>
<div class='field'>
  <%= f.label :Minimo %><br />
  <%= f.text_field :minimo %>
</div>

 
